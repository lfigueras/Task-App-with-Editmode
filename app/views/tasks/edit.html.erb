  
<h3>Edit your task.</h3>
 
<%= form_with model: @task, url:  edit_task_path, local: true do |form| %>
   <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@task.errors.count, "error") %> prohibited
        this task from being saved:
      </h2>
      <ul>
        <% @task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul> 
    </div>
  <% end %>
 

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title, class: "form-control" %>
  </p>
 
  <p>
    <%= form.label :details %><br>
    <%= form.text_area :details, class: "form-control" %>
  </p>
  
<div class="flexi">
    <div class="flexi2"><%= form.submit :Update, class: "btn btn-warning", style:"font-size:10px; "%></div>
    <div class="flexi2"><%= link_to 'Back', category_tasks_path(:category_id), class: "btn btn-primary", style:"font-size:10px;"%></div>
</div>
    
<% end %>
